# Corrected thesis figures 10 September 2026

Figure 3.3 is composed from two editable Mermaid panels using the retained renderer. Figure 3.5 is generated from its single Mermaid source. These are the assets embedded in the 10 September thesis; earlier assets remain unchanged.

The renderer used pinned Mermaid 11.12.0, bundled Node/Playwright, Microsoft Edge and bundled Sharp on the authoring workstation. Its package paths are local runtime paths and must be adjusted on another workstation. Final SVG/PNG files can be used without regeneration. Diagram sequence was checked against agent/main.py, agent/orchestrator.py, agent/tools/notify.py and api/store.py in the current working tree; these are current implementation diagrams, not a new v1 benchmark execution.
