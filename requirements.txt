# Core API
fastapi>=0.112.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.18
httpx>=0.27.0
python-dotenv>=1.0.0
aiosqlite>=0.19.0

# LLM — Gemini Developer API (default provider)
google-genai>=1.0.0

# LLM — optional providers, selected via LLM_PROVIDER
anthropic>=0.60.0
openai>=1.60.0
langchain-anthropic>=0.3.0
langchain-openai>=0.3.0
langchain-google-genai>=4.0.0

# LangChain + LangGraph
langchain>=0.3.0
langchain-core>=0.3.0
langgraph>=0.2.16

# RAG — local vector store + local embeddings (no cloud account required)
chromadb>=0.5.0
langchain-community>=0.3.0
onnxruntime>=1.20.0

# Docker
docker>=7.0.0

# Auth & Security
itsdangerous>=2.1.0
slowapi>=0.1.9

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
respx>=0.21.0
